"use client"; import { useState } from "react"; import { whatsappLink } from "@/lib/utils";
export function ProductInquiry({name,id}:{name:string;id:string}){const [size,setSize]=useState("M"),[color,setColor]=useState("Black"),[position,setPosition]=useState("Front");const href=whatsappLink(process.env.NEXT_PUBLIC_WHATSAPP_NUMBER,`Hello, I am interested in this NAQSH product.\n\nProduct: ${name}\nProduct ID: ${id}\nSize: ${size}\nColor: ${color}\nPrint Position: ${position}\n\nPlease share the details and final price.`);return <div className="mt-7 space-y-5"><div><label className="text-sm font-medium" htmlFor="size">Size</label><select id="size" className="field mt-2" value={size} onChange={e=>setSize(e.target.value)}>{["S","M","L","XL","XXL"].map(x=><option key={x}>{x}</option>)}</select></div><div><label className="text-sm font-medium" htmlFor="color">Colour</label><select id="color" className="field mt-2" value={color} onChange={e=>setColor(e.target.value)}>{["Black","Warm White","Sage"].map(x=><option key={x}>{x}</option>)}</select></div><div><label className="text-sm font-medium" htmlFor="position">Print position</label><select id="position" className="field mt-2" value={position} onChange={e=>setPosition(e.target.value)}>{["Front","Back","Front & back"].map(x=><option key={x}>{x}</option>)}</select></div><a className="button button-clay w-full" href={href} target="_blank" rel="noreferrer">Inquire on WhatsApp</a><p className="text-center text-xs text-neutral-500">Final price and availability are confirmed by our team.</p></div>}
