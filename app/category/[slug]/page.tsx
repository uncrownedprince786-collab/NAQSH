import { demoCategories,demoProducts } from "@/lib/demo"; import { ProductCard } from "@/components/product-card"; import { notFound } from "next/navigation"; export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=demoCategories.find(x=>x.slug===slug);if(!category)return notFound();return <section className="wrap py-12"><p className="eyebrow">Category</p><h1 className="mt-2 text-4xl">{category.name}</h1><p className="mt-3 text-neutral-600">{category.description}</p><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{demoProducts.filter(p=>p.category===slug).map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}
