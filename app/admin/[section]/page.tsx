import Link from"next/link";import {notFound}from"next/navigation";const content:{[key:string]:{title:string;copy:string;actions:string[]}}={products:{title:"Products",copy:"Create, edit, feature, archive and manage product options and imagery.",actions:["Add product","Manage options","Archive safely"]},categories:{title:"Categories",copy:"Add, reorder, show or hide the ways customers browse your catalogue.",actions:["Add category","Set category image","Reorder categories"]},collections:{title:"Collections",copy:"Group products into flexible, curated collections.",actions:["Add collection","Assign products"]},media:{title:"Media library",copy:"Upload images to Cloudinary and reuse them across your catalogue and website.",actions:["Upload image","Choose existing media"]},inquiries:{title:"Inquiries",copy:"View product and custom requests, update progress and keep internal notes.",actions:["Filter by status","Open WhatsApp","Add a note"]},website:{title:"Homepage & pages",copy:"Change hero wording, imagery, homepage sections and page copy without editing code.",actions:["Edit hero","Edit sections","Edit pages"]},settings:{title:"Brand, WhatsApp & contact",copy:"Manage your identity, contact details, sales messages and search previews.",actions:["Brand & logo","WhatsApp messages","Contact details","SEO"]}};export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;const item=content[section];if(!item)return notFound();return <><p className="eyebrow">Admin / {section}</p><h1 className="mt-2 text-3xl">{item.title}</h1><p className="mt-4 max-w-xl leading-7 text-neutral-600">{item.copy}</p><div className="card mt-8 p-6"><p className="font-medium">This area is ready to connect to your Neon database.</p><p className="mt-2 text-sm leading-6 text-neutral-600">Configure the required environment variables, run the migration and seed command, then this content is managed in the database rather than source files.</p><div className="mt-5 flex flex-wrap gap-3">{item.actions.map(x=><button className="button" key={x}>{x}</button>)}</div></div><Link className="mt-6 inline-block text-sm underline" href="/admin">Back to dashboard</Link></>}
