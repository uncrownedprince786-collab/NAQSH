"use client"; import { useState } from "react"; import { whatsappLink } from "@/lib/utils";
export function ProductInquiry({name,id,sizes=["S","M","L","XL","XXL"],colors=["Black","Warm White","Sage"],positions=["Front","Back","Front & back"]}:{name:string;id:string;sizes?:string[];colors?:string[];positions?:string[]}){const [size,setSize]=useState(sizes[0]||"M"),[color,setColor]=useState(colors[0]||"Black"),[position,setPosition]=useState(positions[0]||"Front"),[quantity,setQuantity]=useState("1");const href=whatsappLink(process.env.NEXT_PUBLIC_WHATSAPP_NUMBER,`Hello, I am interested in this NAQSH product.\n\nProduct: ${name}\nProduct ID: ${id}\nQuantity: ${quantity}\nSize: ${size}\nColor: ${color}\nPrint Position: ${position}\n\nPlease share the details and final price.`);return <div className="mt-7 space-y-5"><div className="grid gap-5 sm:grid-cols-2"><div><label className="text-sm font-medium" htmlFor="size">Size</label><select id="size" className="field mt-2" value={size} onChange={e=>setSize(e.target.value)}>{sizes.map(x=><option key={x}>{x}</option>)}</select></div><div><label className="text-sm font-medium" htmlFor="quantity">Quantity</label><input id="quantity" className="field mt-2" min="1" type="number" value={quantity} onChange={e=>setQuantity(e.target.value)}/></div></div><div><label className="text-sm font-medium" htmlFor="color">Colour</label><select id="color" className="field mt-2" value={color} onChange={e=>setColor(e.target.value)}>{colors.map(x=><option key={x}>{x}</option>)}</select></div><div><label className="text-sm font-medium" htmlFor="position">Print position</label><select id="position" className="field mt-2" value={position} onChange={e=>setPosition(e.target.value)}>{positions.map(x=><option key={x}>{x}</option>)}</select></div><a className="button button-clay w-full" href={href} target="_blank" rel="noreferrer">Inquire on WhatsApp</a><p className="text-center text-xs text-neutral-500">Final price and availability are confirmed by our team.</p></div>}
