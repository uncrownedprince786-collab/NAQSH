import Image from "next/image"; import Link from "next/link"; import { money } from "@/lib/utils";
export function ProductCard({product}:{product:{name:string;slug:string;price:number|string;image:string}}){return <Link href={`/product/${product.slug}`} className="group block"><div className="relative aspect-[4/5] overflow-hidden bg-line"><Image src={product.image} alt={product.name} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 768px) 50vw, 25vw"/></div><div className="mt-3 flex justify-between gap-3"><h3 className="font-medium">{product.name}</h3><span className="shrink-0 text-sm">{money(product.price)}</span></div></Link>}
