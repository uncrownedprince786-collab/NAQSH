import { NextResponse } from "next/server"; import { prisma } from "@/lib/db"; import { isAdmin } from "@/lib/auth";
export async function GET() { if (!(await isAdmin())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); return NextResponse.json(await prisma.inquiry.findMany({ orderBy: { createdAt: "desc" }, take: 100 })); }
export async function PATCH(request: Request) { if (!(await isAdmin())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const body = await request.json(); if (typeof body.id !== "string" || typeof body.status !== "string") return NextResponse.json({ error: "Invalid inquiry update." }, { status: 400 }); return NextResponse.json(await prisma.inquiry.update({ where: { id: body.id }, data: { status: body.status as never } })); }
