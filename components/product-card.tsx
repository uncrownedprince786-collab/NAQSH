import Image from "next/image"; import Link from "next/link"; import { money } from "@/lib/utils";
export function ProductCard({product}:{product:{name:string;slug:string;price:number|string;image:string;categoryLabel?:string}}){return <Link href={`/product/${product.slug}`} className="group block"><div className="relative aspect-[4/5] overflow-hidden bg-line"><Image src={product.image} alt={product.name} fill className="image-lift object-cover" sizes="(max-width: 768px) 50vw, 25vw"/></div><div className="mt-4 flex justify-between gap-3 border-b border-line pb-4"><div><p className="text-xs tracking-wide text-sage">{product.categoryLabel}</p><h3 className="mt-1 font-medium leading-snug">{product.name}</h3></div><span className="shrink-0 pt-1 text-sm">{money(product.price)}</span></div></Link>}
