"use client";import {FormEvent,useState}from"react";import {useRouter}from"next/navigation";export default function Login(){const r=useRouter(),[error,setError]=useState("");async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const x=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(x)});if(res.ok)r.push("/admin");else setError("Incorrect email or password.")}return <main className="mx-auto flex min-h-screen max-w-md items-center p-6"><form onSubmit={go} className="card w-full p-7"><p className="eyebrow">NAQSH administration</p><h1 className="mt-2 text-3xl">Sign in</h1><label className="mt-6 block text-sm">Email<input name="email" type="email" className="field mt-2" required/></label><label className="mt-4 block text-sm">Password<input name="password" type="password" className="field mt-2" required/></label>{error&&<p className="mt-3 text-sm text-red-700">{error}</p>}<button className="button button-clay mt-6 w-full">Sign in</button></form></main>}
