import { ProductCard } from "@/components/product-card"; import { liveProducts } from "@/lib/catalog"; export const metadata={title:"Shop"}; export default async function Shop(){const products=await liveProducts();return <section className="wrap py-12"><p className="eyebrow">Browse the studio</p><h1 className="mt-2 text-4xl">Products</h1><p className="mt-3 max-w-xl text-neutral-600">Find a piece to make your own, or start with a custom request.</p><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}
