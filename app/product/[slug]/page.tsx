import Image from "next/image"; import { demoProducts } from "@/lib/demo"; import { notFound } from "next/navigation"; import { money } from "@/lib/utils"; import { ProductInquiry } from "@/components/product-inquiry"; export default async function Product({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=demoProducts.find(x=>x.slug===slug);if(!p)return notFound();return <section className="wrap grid gap-10 py-10 md:grid-cols-2"><div className="relative aspect-[4/5] overflow-hidden bg-line"><Image src={p.image} alt={p.name} fill priority className="object-cover" sizes="(max-width:768px) 100vw,50vw"/></div><div className="max-w-lg py-2"><p className="eyebrow">Made to order</p><h1 className="mt-3 text-4xl">{p.name}</h1><p className="mt-3 text-lg">{money(p.price)}</p><p className="mt-6 leading-7 text-neutral-600">{p.description}</p><div className="mt-6 border-l-2 border-sage pl-4 text-sm text-neutral-600">Customizable — share your own artwork or ask us to adapt the placement.</div><ProductInquiry name={p.name} id={p.id}/></div></section>}
