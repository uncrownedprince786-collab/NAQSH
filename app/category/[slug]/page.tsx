import Link from "next/link";
import { notFound } from "next/navigation";
import { ProductCard } from "@/components/product-card";
import { prisma } from "@/lib/db";
import { demoProducts } from "@/lib/demo";
export default async function Category({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; try { const category = await prisma.category.findFirst({ where: { slug, isVisible: true }, include: { children: { where: { isVisible: true }, orderBy: { position: "asc" }, include: { products: { where: { isActive: true }, include: { images: { include: { media: true }, orderBy: { position: "asc" } } } } } }, products: { where: { isActive: true }, include: { images: { include: { media: true }, orderBy: { position: "asc" } } } } } }); if (category) { const products = [...category.products, ...category.children.flatMap((child) => child.products)]; return <section className="wrap py-12"><p className="eyebrow">Category</p><h1 className="mt-2 text-4xl">{category.name}</h1><p className="mt-3 text-neutral-600">{category.description}</p>{category.children.length > 0 && <div className="mt-7 flex flex-wrap gap-2">{category.children.map((child) => <Link key={child.id} href={`/category/${child.slug}`} className="button !px-4 !py-2 text-sm">{child.name}</Link>)}</div>}<div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <ProductCard key={product.id} product={{ name: product.name, slug: product.slug, price: Number(product.price), image: product.images[0]?.media.url || demoProducts[0].image }} />)}</div></section>; } } catch { /* local demo fallback below */ } const products = demoProducts.filter((product) => product.category === slug); if (!products.length) return notFound(); return <section className="wrap py-12"><p className="eyebrow">Category</p><h1 className="mt-2 text-4xl">{slug}</h1><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => <ProductCard key={product.id} product={product} />)}</div></section>; }
