import type { Metadata } from "next";
import { ProductCard } from "@/components/product-card";
import { liveCollection } from "@/lib/catalog";
import { notFound } from "next/navigation";
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const collection = await liveCollection((await params).slug); return collection ? { title: `${collection.name} Designs`, description: `Explore ${collection.name.toLowerCase()} custom apparel and merchandise designs from NAQSH.`, alternates: { canonical: `/collection/${(await params).slug}` } } : {}; }
export default async function Collection({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const collection = await liveCollection(slug); if (!collection) return notFound(); return <section className="wrap py-12"><p className="eyebrow">Design collection</p><h1 className="mt-2 text-4xl">{collection.name}</h1><p className="mt-3 max-w-xl text-neutral-600">Ready-made design ideas that you can wear as they are or use as a starting point for something personal.</p><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{collection.products.map((product) => <ProductCard key={product.id} product={product} />)}</div></section>; }
