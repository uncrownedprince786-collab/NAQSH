import Image from "next/image"; import Link from "next/link"; import { money } from "@/lib/utils";
export function ProductCard({product}:{product:{name:string;slug:string;price:number|string;image:string;categoryLabel?:string}}){return <Link href={`/product/${product.slug}`} className="group block"><div className="relative aspect-[3/4] overflow-hidden bg-sand"><Image src={product.image} alt={product.name} fill className="image-lift object-cover" sizes="(max-width: 768px) 50vw, 25vw"/></div><div className="mt-5 flex justify-between gap-3 border-b border-current/30 pb-4"><div><p className="text-[.62rem] font-bold uppercase tracking-[.14em] opacity-60">{product.categoryLabel}</p><h3 className="mt-2 font-bold uppercase tracking-[.04em] leading-snug">{product.name}</h3></div><span className="shrink-0 pt-1 text-sm">{money(product.price)}</span></div></Link>}
