import { demoProducts } from "@/lib/demo";import { ProductCard } from "@/components/product-card";export default async function Collection({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <section className="wrap py-12"><p className="eyebrow">Collection</p><h1 className="mt-2 text-4xl capitalize">{slug.replaceAll("-"," ")}</h1><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{demoProducts.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}
