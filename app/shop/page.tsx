import { demoProducts } from "@/lib/demo"; import { ProductCard } from "@/components/product-card"; export const metadata={title:"Shop"}; export default function Shop(){return <section className="wrap py-12"><p className="eyebrow">Browse the studio</p><h1 className="mt-2 text-4xl">Products</h1><p className="mt-3 max-w-xl text-neutral-600">Demo catalogue content is clearly marked and can be replaced in the admin panel.</p><div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{demoProducts.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}
