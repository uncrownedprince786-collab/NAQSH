"use client";
import { FormEvent, useEffect, useState } from "react";
export default function CollectionsAdmin() { const [items, setItems] = useState<any[]>([]); const [message, setMessage] = useState(""); useEffect(() => { fetch("/api/admin/collections").then((r) => r.json()).then(setItems); }, []); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const data = Object.fromEntries(new FormData(e.currentTarget)); const response = await fetch("/api/admin/collections", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); setMessage(response.ok ? "Collection created." : "Could not create collection."); if (response.ok) setItems(await fetch("/api/admin/collections").then((r) => r.json())); } return <><p className="eyebrow">Catalogue</p><h1 className="mt-2 text-3xl">Collections</h1><div className="mt-8 grid gap-8 md:grid-cols-[1fr_320px]"><div className="card">{items.map((item) => <div className="flex justify-between border-b border-line p-4" key={item.id}><span>{item.name}</span><span className="text-sm text-neutral-500">{item.isVisible ? "Visible" : "Hidden"} · {item.position}</span></div>)}</div><form onSubmit={submit} className="card p-5"><h2 className="text-xl">Add collection</h2><label className="mt-4 block text-sm">Name<input className="field mt-1" name="name" required /></label><label className="mt-4 block text-sm">Description<input className="field mt-1" name="description" /></label><label className="mt-4 block text-sm">Position<input className="field mt-1" name="position" type="number" defaultValue="0" /></label><label className="mt-4 flex gap-2 text-sm"><input type="checkbox" name="isVisible" defaultChecked /> Visible</label>{message && <p className="mt-3 text-sm">{message}</p>}<button className="button button-clay mt-5">Create collection</button></form></div></>; }
