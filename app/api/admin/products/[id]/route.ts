import { NextResponse } from "next/server"; import { prisma } from "@/lib/db"; import { isAdmin } from "@/lib/auth"; import { z } from "zod";
const schema = z.object({ name: z.string().min(2), slug: z.string().min(2), description: z.string().min(2), price: z.coerce.number().nonnegative(), categoryId: z.string().cuid(), isCustomizable: z.boolean(), isFeatured: z.boolean(), isActive: z.boolean(), sizes: z.array(z.string()).default([]), colors: z.array(z.string()).default([]), printPositions: z.array(z.string()).default([]), imageIds: z.array(z.string().cuid()).default([]) });
export async function GET(_: Request, { params }: { params: Promise<{ id: string }> }) { if (!(await isAdmin())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const { id } = await params; const product = await prisma.product.findUnique({ where: { id }, include: { variants: true, category: true, images: { include: { media: true } } } }); return product ? NextResponse.json(product) : NextResponse.json({ error: "Not found" }, { status: 404 }); }
export async function PUT(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!(await isAdmin())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const { id } = await params; try { const body = schema.parse(await request.json()); const variant = body.sizes.length || body.colors.length || body.printPositions.length ? { upsert: { where: { id: (await prisma.productVariant.findFirst({ where: { productId: id } }))?.id || "missing" }, create: { sizes: body.sizes, colors: body.colors, printPositions: body.printPositions }, update: { sizes: body.sizes, colors: body.colors, printPositions: body.printPositions } } } : undefined; const product = await prisma.product.update({ where: { id }, data: { name: body.name, slug: body.slug, description: body.description, price: body.price, categoryId: body.categoryId, isCustomizable: body.isCustomizable, isFeatured: body.isFeatured, isActive: body.isActive, variants: variant, images: { deleteMany: {}, create: body.imageIds.map((mediaId, position) => ({ mediaId, position })) } } }); return NextResponse.json(product); } catch { return NextResponse.json({ error: "Could not update product." }, { status: 400 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { if (!(await isAdmin())) return NextResponse.json({ error: "Unauthorized" }, { status: 401 }); const { id } = await params; const product = await prisma.product.update({ where: { id }, data: { isActive: false, archivedAt: new Date() } }); return NextResponse.json(product); }
